import { GetIntialsPipe } from './get-intials-pipe';

describe('GetIntialsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetIntialsPipe();
    expect(pipe).toBeTruthy();
  });
});
